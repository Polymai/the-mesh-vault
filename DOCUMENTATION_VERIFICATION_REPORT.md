# TheMeshVault protocol-v3 documentation verification

Date: 2026-08-16  
Scope: current public, technical and machine-readable documentation compared with protocol v3 and the bounded Patient Zero first-contact gate.

## Status

The current source enforces a narrower model than the previous durable-fallback design. Every active node can participate in the signed control mesh. An ordinary local profile may use Supabase only during one bounded introduction to the deployment-pinned Patient Zero identity. After a verified mesh peer exists, it deletes its temporary node row best-effort, clears its anonymous session, closes Realtime and records a permanent local completion marker. Patient Zero alone keeps a minimal listener and sparse heartbeat while operator-enabled.

## Current implementation boundaries

| Claim | Current implementation evidence | Status |
|---|---|---|
| A new Mesh Key contains 256 bits generated by the browser CSPRNG and encoded as a 24-word BIP39 mnemonic with checksum. | `js/identity/mnemonic.js` | Implemented in source |
| The same valid Mesh Key deterministically derives the same Ed25519 owner identity and self-certifying Vault ID on another browser. | `js/identity/vault-identity.js`, `js/security/signing.js` | Implemented in source |
| Root material and owner private-key material are kept in the authorized browser profile; protocol v3 does not publish recovery envelopes or wrapped segment keys. | `js/identity/vault-identity.js`, `js/security/keyring.js`, `supabase/schema.sql`, `data/schema.json` | Implemented in source |
| Metadata, deduplication and segment-key roots use domain-separated HKDF derivations from the Mesh Key root. | `js/security/signing.js`, `js/security/keyring.js` | Implemented in source |
| Each segment key is derived locally from signed segment context; every segment descriptor and manifest is owner-signed with Ed25519. | `js/security/keyring.js`, `js/security/segment-descriptor.js`, `js/security/manifest.js` | Implemented in source |
| Files use fixed 8 MiB logical segments, optional gzip when it saves at least 5%, AES-256-GCM per segment and per-segment Reed-Solomon coding. | `data/runtime-config.js`, `js/security/compression.js`, `js/flows/upload-flow.js` | Implemented in source |
| Supabase receives only temporary anonymous Auth/node data, an exact Patient Zero lookup and recipient-scoped first-contact signaling from an ordinary profile. It does not receive vault catalogs, manifests, placements, repair/deletion state, share capabilities or shard bytes. | `js/network/supabase-fallback-policy.js`, `js/network/node-service.js`, provider source, schema/RLS | Implemented source contract; fresh live traffic inspection pending |
| WebRTC carries encrypted shard traffic directly between nodes. After first-peer verification, the ordinary profile cannot reopen Supabase for discovery, signaling, presence or Live Mesh. | network/services source and first-contact policy | Implemented source contract |
| Every active node automatically caches a bounded set of signed control records. An opt-in Backbone advertises a longer-lived control endpoint, larger cache and stronger availability commitment; it is not a trusted master. | `js/network/anchor-service.js`, `js/network/coordination-policy.js`, runtime configuration | Implemented source contract |
| A completely new profile uses a narrow RPC to find only the deployment-pinned Patient Zero public key; it cannot enumerate a general hosted node directory. | Supabase bootstrap provider, schema and policy checks | Implemented source contract |
| The dedicated Node.js Backbone host can run multiple logical instances with separate quotas and storage roots, but all instances on one physical host share one failure domain and never count as independent shard placements. | `native/backbone-host/`, placement and failure-domain source | Implemented and locally tested |
| Explicit share links disclose only the limited encrypted segment-key grants required by that capability; the URL fragment is the share secret. They do not disclose the Mesh Key root. | `js/security/capability.js`, share flows | Implemented source contract |
| Full phrase/file restore opens the same v3 Vault ID without sending or logging the Mesh Key. | `POLYMAI_MESH_KEY_PORTABILITY_CHECK.cjs` | PASS after provisioning |
| Four isolated browser profiles can discover and connect to peers under the provisioned contract. | `POLYMAI_MESH_VIEWS_CHECK.cjs` | PASS after provisioning; not a shard-recovery proof |
| PWA share-target and camera fixtures enter the real upload path. | `POLYMAI_PWA_FILE_INTEGRATION_CHECK.cjs` | PASS on rerun after one transient network failure; not a multi-segment recovery proof |
| Installed-PWA and ordinary-tab instances resume heartbeat/discovery on foreground return; same-profile windows share discovered-peer snapshots while direct WebRTC state remains window-local. | `js/bootstrap.js`, `js/network/peer-registry.js`, `js/platform/service-worker.js`, `POLYMAI_PWA_INSTALLABILITY_CHECK.cjs`, `POLYMAI_MESH_VIEWS_CHECK.cjs` | PASS in local PWA/browser and four-profile mesh checks; not a mobile-background guarantee |

## Documentation inventory result

- `js/content/public-documents.js`, `ai-manifest.json`, the defensive publication, release/UAT reports and test plan now identify the active contract as protocol v3.
- `DOCUMENTATION_INDEX.md` separates current sources of truth from contributor instructions and historical records.
- The initial Polymai task/mirror, resolved provisioning incident and chronological changelog remain available for traceability but are explicitly labelled as superseded where they describe pre-v3 behavior.
- No current public document describes hosted recovery envelopes, wrapped segment keys, pre-v3 restore compatibility or uninterrupted mobile-background execution as implemented behavior.

## Security statements that must remain qualified

- “Keys never leave the device” applies to the Mesh Key root and owner private signing material during ordinary vault operation. A user who deliberately creates a share link exports a limited capability containing segment access grants.
- The root seed is stored in the origin's private IndexedDB so the vault can reopen. A compromised browser profile, device or same-origin script can therefore compromise it.
- Temporary anonymous Supabase Auth is pseudonymous, not invisible: Supabase can process IP addresses, user agent, timing, temporary identifiers and transient SDP/ICE during first contact. Provider logs/backups may outlive best-effort row deletion.
- Signed and hashed cache records provide integrity checks; permissive anonymous cache writes can still create availability or abuse risk.
- A Mesh Key provides authority and deterministic key derivation. File recovery also requires enough valid, reachable shards.
- PWA installation, Wake Lock, notifications and Backbone mode do not guarantee that a mobile operating system will keep a browser process alive.
- The optional Android foreground service is more durable than a PWA but remains subject to force-stop, Doze, network loss and OEM battery policy. Its native worker receives public node configuration and opaque storage/control objects, never the Mesh Key or root-derived file keys.

## Removed obsolete claims

The current release documentation must not describe any of these as protocol-v3 behavior:

- recovery envelopes in Supabase or on Anchors;
- wrapped per-segment decryption keys in manifests or coordination storage;
- protocol-v1/v2, ECDSA P-256 or six-group legacy recovery compatibility;
- a complete cold-start recovery proof for the new v3 format;
- provisioning success as proof that live peer transfer, recovery, deletion or mobile continuity has passed.

## Verification still required

1. Run a fresh browser/PWA/Android first-contact journey against the deployed origin and verify the exact pinned Patient Zero peer.
2. Inspect browser state and provider traffic to prove best-effort row deletion, local session clearing and zero ordinary Supabase calls after completion and reload.
3. Repeat creation, reload and Mesh-Key portability checks in Chromium, Firefox and WebKit.
4. Run uploader-offline golden recovery, K/K-1 threshold, TURN-only, mobile suspension, shard repair and deletion tests.
5. Install the verified APK on physical Android devices and execute the screen-off, reboot, stop, force-stop, native storage and first-contact inspection matrix in `ANDROID_BACKGROUND_NODE.md`.

Until those gates pass, protocol v3 is a **provisioned pre-release candidate**, not a production-verified release.
